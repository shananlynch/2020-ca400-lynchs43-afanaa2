[int] insert100([int] a){
     var int i = 0 ;
     while(i < 100 ){
          a.insert(i);
          i= i + 1;
     }
    return (a) ;
}

main{
    var [int] l =[];
    var [int] i = insert100(l) ;
    var int j = 0;
    while(j < 100 ){
          var int k = i.get(j);
          j = j + 1 ;
          print k;
    }
}
