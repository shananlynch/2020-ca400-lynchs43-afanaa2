[string] insert100([string] a){
     var int i = 0 ;
     while(i < 100 ){
          a.insert("hello");
          i= i + 1;
     }
    return (a) ;
}

main{
    var [string] l =[];
    var [string] i = insert100(l) ;
    var int j = 0;
    while(j < 100 ){
          var string k = i.get(9);
          j = j + 1 ;
          print k;
    }
}
